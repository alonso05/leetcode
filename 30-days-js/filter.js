const fn = function greaterThan10(n) { return n > 10; }
const arr = [0,10,20,30]

const filter = function() {
    // const filterApplied = []
    // for(i=0; i<arr.length; i++){
    //     if(fn(arr[i]),i)
    //       filterApplied.push(arr[i])  
    // }
    console.log('filtering')
};

const filtered = filter()

filtered()